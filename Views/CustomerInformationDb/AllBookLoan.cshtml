@model List<Book>

<h2>Böcker som kunden har lånat</h2>

@if (Model.Count > 0)
{
    <ul>
        @foreach (var book in Model)
        {
            <li>@book.Title</li>
        }
    </ul>
}
else
{
    <p>Kunden har inte lånat några böcker.</p>
}